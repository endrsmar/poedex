"""`moddb` — core module. A trimmed Path of Exile mod database.

Facts about mods: which affixes exist, what each rolls, where each can spawn, which
influence pool it belongs to, and what tier a given roll is *on a given base*.

Core because none of that is an opinion. Whether a T1 roll makes an item worth
keeping is `appraisal`'s job and stays there.
"""
